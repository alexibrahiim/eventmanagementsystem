<%- include('../partials/header') %>

<main class="event-form-page">
    <section class="event-form-shell">

        <div class="form-back-row">
            <a href="/categories" class="form-back-link">← Back to Categories</a>
        </div>

        <div class="event-form-card">

            <div class="event-form-header">
                <span class="form-badge">Admin Panel</span>
                <h1>Edit Category</h1>
                <p>
                    Update this category information. Any changes will affect how events under this category are organized and displayed.
                </p>
            </div>

            <% if (typeof error !== 'undefined' && error) { %>
                <div class="error-message">
                    <%= error %>
                </div>
            <% } %>

            <form action="/categories/edit/<%= category._id %>" method="POST" class="event-form">

                <div class="form-group">
                    <label for="name">Category Name</label>
                    <input
                        type="text"
                        id="name"
                        name="name"
                        value="<%= category.name %>"
                        placeholder="Example: Technology"
                        required
                    >
                </div>

                <div class="form-group">
                    <label for="description">Description</label>
                    <textarea
                        id="description"
                        name="description"
                        rows="5"
                        placeholder="Write a short description about this category"
                    ><%= category.description || '' %></textarea>
                </div>

                <div class="form-actions">
                    <button type="submit" class="btn-save-event">
                        Update Category
                    </button>

                    <a href="/categories" class="btn-cancel-form">
                        Cancel
                    </a>
                </div>

            </form>

        </div>

    </section>
</main>

<%- include('../partials/footer') %>